# Cloud sync (optional). Copy to .env.local and fill in to enable
# Supabase-backed multi-device / multi-user sync + email login.
# Leave blank / unset to run in local-only mode (IndexedDB, this device).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
